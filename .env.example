# Cloud connectivity + DB pass-through pipe + PayPal only

# DB pass-through pipe (required for user UI and API calls)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional — absolute site URL (e.g. for emails)
NEXT_PUBLIC_SITE_URL=https://your-domain.com
